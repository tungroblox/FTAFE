export interface CollectedHub {
    id: string;
    code: string;
    name: string;
    description: string;
    image: string;
    address: string;
    status: string;
    createdAt: string;
    updatedAt: string;
}
